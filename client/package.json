{
  "name": "powercontrol-client",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "android": "vue-tsc --noEmit && vite build && cap run android",
    "androidgradlew": "vue-tsc --noEmit && vite build && cap sync android && cd android && ./gradlew assembleDebug",
    "resources": "cordova-res android --skip-config --copy"
  },
  "dependencies": {
    "@capacitor/android": "^3.3.4",
    "@capacitor/core": "^3.3.4",
    "@robingenz/capacitor-android-dark-mode-support": "^1.0.0",
    "redaxios": "^0.4.1",
    "vue": "^3.2.25"
  },
  "devDependencies": {
    "@capacitor/cli": "^3.3.4",
    "@vitejs/plugin-vue": "^2.0.0",
    "cordova-res": "^0.15.4",
    "typescript": "^4.4.4",
    "vite": "^2.7.2",
    "vite-plugin-windicss": "^1.6.1",
    "vue-tsc": "^0.30.2",
    "windicss": "^3.4.2"
  }
}
