import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mirsella.powercontrol',
  appName: 'powercontrol',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
